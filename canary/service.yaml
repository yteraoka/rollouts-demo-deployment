---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-canary-demo
  labels:
    app: rollouts-canary-demo
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: rollouts-canary-demo
